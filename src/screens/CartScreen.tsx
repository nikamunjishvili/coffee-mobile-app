import React from 'react';
import {CartLayout} from '../components/layouts';

const CartScreen = () => {
  const title = 'Cart Screen';
  return <CartLayout title={title} />;
};

export default CartScreen;

import React from 'react';
import {DetailsLayout} from '../components/layouts';

const DetailsScreen = () => {
  const title = 'Details Screen';
  return <DetailsLayout title={title} />;
};

export default DetailsScreen;

import React from 'react';
import {FavoriteLayout} from '../components/layouts';

const FavoriteScreen = () => {
  const title = 'Favorite Screen';
  return <FavoriteLayout title={title} />;
};

export default FavoriteScreen;
